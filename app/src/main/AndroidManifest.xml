<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myalphabet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ExrQuestionActivity"
            android:parentActivityName=".ExrActivity" />
        <activity android:name=".ExrReadActivity"
            android:parentActivityName=".ExrActivity" />
        <activity android:name=".ZhattygularActivity"
            android:parentActivityName=".ExrActivity" />
        <activity android:name=".ExrWriteActivity"
            android:parentActivityName=".ExrActivity" />
        <activity
            android:name=".OneTaskActivity"
            android:parentActivityName=".ExrActivity" />
        <activity
            android:name=".ExrActivity"
            android:parentActivityName=".AlphabetActivity" />
        <activity
            android:name=".CategoryActivity"
            android:parentActivityName=".CategoryActivity" />
        <activity android:name=".Main2Activity" />
        <activity
            android:name=".LetterDetailActivity"
            android:parentActivityName=".AlphabetActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AlphabetActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>